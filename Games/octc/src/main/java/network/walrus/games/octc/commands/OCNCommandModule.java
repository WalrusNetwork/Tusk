package network.walrus.games.octc.commands;

import app.ashcon.intake.parametric.binder.Binder;
import network.walrus.games.core.commands.GamesCoreCommandModule;

/**
 * Command module for provisions for all commands used in the ocn external component.
 *
 * @author Austin Mayes
 */
public class OCNCommandModule extends GamesCoreCommandModule {

  @Override
  public void configure(Binder binder) {
    super.configure(binder);
  }
}
